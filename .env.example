# Copie para .env e ajuste os valores antes de rodar docker-compose
# Este arquivo NÃO deve ser commitado com valores reais

# Senha do PostgreSQL (troque para algo seguro)
DB_PASSWORD=pedidobk2026

# GCS (já configurado para o projeto existente)
GCS_BUCKET_NAME=financeiro-00-bucket
GCS_PROJECT_ID=financeiro-489122
GCS_PREFIX=pedidos

# Storage: "gcs" em produção
STORAGE_BACKEND=gcs

# Usuário do host para montar ADC (gcloud auth)
HOST_USER=jcmalinski

LOG_LEVEL=INFO
